// 部署时此文件由 GitHub Actions 自动生成
// 本地开发时请手动修改

// API 地址
const API_BASE = 'https://your-worker-name.your-account.workers.dev';

// 显示配置（与后端配置保持一致）
const DISPLAY_CONFIG = {
  expireHours: 12,               // 图片过期时间（小时）
  maxFileSizeMB: 20,             // 单文件大小限制（MB）
  allowedTypesDisplay: 'JPG、PNG、GIF、WebP、SVG', // 允许的文件类型显示文本
};
