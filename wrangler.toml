name = "flyimg-worker"
main = "worker.js"
compatibility_date = "2025-02-01"
workers_dev = true

# R2 存储桶绑定（固定名称为 flyimg）
[[r2_buckets]]
binding = "R2_BUCKET"
bucket_name = "flyimg"

# 定时触发器（每小时执行一次清理）
[triggers]
crons = ["0 */1 * * *"]

# Pages 构建输出目录
pages_build_output_dir = "frontend"
